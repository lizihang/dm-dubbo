<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dm.goods.dao.GoodsDAO">

	<select id="queryGoodsList" resultType="Goods" parameterType="GoodsQueryParams">
        select
            id, goods_name, price, quantity, remark, create_user, create_time, modify_user, modify_time
        from
            dm_goods
        <where>
            <if test="goodsName != null">
                and goods_name like CONCAT('%',#{goodsName,jdbcType=VARCHAR},'%')
            </if>
        </where>
    </select>

    <select id="queryTotal" resultType="int">
        select count(1) from dm_goods
        <where>
            <if test="goodsName != null">
                and goods_name like CONCAT('%',#{goodsName,jdbcType=VARCHAR},'%')
            </if>
        </where>
    </select>
</mapper>